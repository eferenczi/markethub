import React, { useEffect, useState } from "react";
import { ClipboardList, FileCheck2, Layers3, Loader2, MapPinned, Plus, Save } from "lucide-react";
import { api } from "./api";
import { C, FD } from "./theme";

const inp = { background: C.card, border: `1px solid ${C.line}`, color: C.ink };
const TYPES = [{ key: "booths", label: "Booth Layouts", icon: MapPinned }, { key: "onboarding", label: "Onboarding Forms", icon: ClipboardList }, { key: "categories_spaces", label: "Categories & Spaces", icon: Layers3 }, { key: "required_documents", label: "Required Documents", icon: FileCheck2 }];
const hint = { onboarding: "One vendor acknowledgment question per line.", categories_spaces: "Enter category or space options, one per line.", required_documents: "Enter requested document names, one per line. These are displayed as requested—not forced—on the vendor application." };

export default function TemplatesPage({ canWrite, notify }) {
  const [type, setType] = useState("booths"); const [templates, setTemplates] = useState([]); const [markets, setMarkets] = useState([]); const [name, setName] = useState(""); const [items, setItems] = useState(""); const [marketId, setMarketId] = useState(""); const [busy, setBusy] = useState(false);
  const load = async () => { try { const [templateResult, marketResult] = await Promise.all([api.getTemplates(), api.getMarkets()]); setTemplates(templateResult.templates); setMarkets(marketResult.markets.filter((market) => !market.archived)); } catch (error) { notify(error.message, "err"); } };
  useEffect(() => { load(); }, []);
  const current = templates.filter((template) => template.type === type);
  const save = async () => { if (!name.trim() || !items.trim()) return notify("Add a template name and at least one item", "err"); setBusy(true); try { const lines = items.split("\n").map((item) => item.trim()).filter(Boolean); const config = type === "onboarding" ? { questions: lines.map((label, index) => ({ id: `q${index + 1}`, label, type: "acknowledgment" })) } : { items: lines }; await api.createTemplate({ type, name: name.trim(), config }); setName(""); setItems(""); await load(); notify("Template saved"); } catch (error) { notify(error.message, "err"); } finally { setBusy(false); } };
  const assign = async (template) => { if (!marketId) return notify("Choose a market to apply this template", "err"); try { await api.assignTemplate(template.id, { market_id: Number(marketId) }); notify(`${template.name} applied to this market`); } catch (error) { notify(error.message, "err"); } };
  const display = (template) => (template.config.questions || template.config.items || []).map((item) => typeof item === "string" ? item : item.label).join(" · ");
  return <div className="flex flex-col gap-4"><section style={{ background: C.card, border: `1px solid ${C.line}` }} className="rounded-2xl p-4"><p style={{ fontFamily: FD }} className="text-[20px] font-semibold">Organizer templates</p><p style={{ color: C.sub }} className="text-[12.5px] mt-1">Save the building blocks you reuse across markets. Booth layouts are managed in Event Operations; the other templates can be applied to a market or, for onboarding, to a specific event.</p><div className="flex flex-wrap gap-2 mt-4">{TYPES.map(({ key, label, icon: Icon }) => <button key={key} onClick={() => setType(key)} style={{ background: type === key ? C.pine : C.paper2, color: type === key ? "#fff" : C.sub }} className="px-3 py-2 rounded-lg text-[12px] font-bold flex items-center gap-1.5"><Icon size={14} /> {label}</button>)}</div></section>{type === "booths" ? <section style={{ background: C.card, border: `1px solid ${C.line}` }} className="rounded-2xl p-5"><MapPinned size={26} color={C.pine} /><p style={{ fontFamily: FD }} className="text-[19px] font-semibold mt-2">Booth layouts</p><p style={{ color: C.sub }} className="text-[13px] mt-1">Upload a venue image and create 10×10 canopy or food-truck spots in Event Operations → Booth map. Each market’s latest layout is automatically used for new future event dates.</p></section> : <><section style={{ background: C.card, border: `1px solid ${C.line}` }} className="rounded-2xl p-4"><p style={{ fontFamily: FD }} className="text-[18px] font-semibold">Create {TYPES.find((item) => item.key === type)?.label}</p><p style={{ color: C.sub }} className="text-[12px] mt-1">{hint[type]}</p>{canWrite && <div className="flex flex-col gap-2 mt-3"><input value={name} onChange={(event) => setName(event.target.value)} placeholder="Template name" style={inp} className="px-3 py-2.5 rounded-lg text-[13px] outline-none" /><textarea value={items} onChange={(event) => setItems(event.target.value)} rows={5} placeholder={type === "onboarding" ? "I understand the arrival time and setup rules.\nI confirm my booth will be staffed for the full market." : "Add one item per line"} style={inp} className="px-3 py-2.5 rounded-lg text-[13px] outline-none resize-y" /><button onClick={save} disabled={busy} style={{ background: C.pine, color: "#fff" }} className="self-start px-3 py-2 rounded-lg text-[12px] font-bold flex items-center gap-1"><Save size={14} /> Save template</button></div>}</section><section className="flex flex-col gap-2"><div className="flex gap-2 items-center"><select value={marketId} onChange={(event) => setMarketId(event.target.value)} style={inp} className="px-3 py-2 rounded-lg text-[12px] outline-none"><option value="">Choose market to apply templates…</option>{markets.map((market) => <option key={market.id} value={market.id}>{market.name}</option>)}</select></div>{current.length === 0 && <p style={{ color: C.faint }} className="text-center py-5 text-[13px]">No saved templates in this category yet.</p>}{current.map((template) => <article key={template.id} style={{ background: C.card, border: `1px solid ${C.line}` }} className="rounded-xl p-4"><div className="flex gap-3 items-start"><div className="flex-1"><p className="text-[14px] font-bold">{template.name}</p><p style={{ color: C.sub }} className="text-[12px] mt-1">{display(template) || "No items"}</p></div>{canWrite && <button onClick={() => assign(template)} style={{ background: C.sageSoft, color: C.pine }} className="px-3 py-1.5 rounded-lg text-[11.5px] font-bold">Apply to market</button>}</div></article>)}</section></>}</div>;
}
